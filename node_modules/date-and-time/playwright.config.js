const { defineConfig } = require('@playwright/test');

module.exports = defineConfig({
    testDir: './tests',
    reporter: 'list',
    use: {
        baseURL: 'http://127.0.0.1:3000/tests/'
    },
    webServer: {
        command: 'npx http-server -c-1 -p 3000',
        url: 'http://127.0.0.1:3000',
        reuseExistingServer: true
    }
});
